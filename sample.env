# Vite only exposes variables prefixed with VITE_ to client code.
# See https://vite.dev/guide/env-and-mode

# Google analytics ID. Analytics only initialise in production builds.
# VITE_GA_TRACKING_ID=UA_XXXXXX
